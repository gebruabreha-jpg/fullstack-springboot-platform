apiVersion: v1
kind: ConfigMap
metadata:
  name: fake-worker-config
  namespace: kwok-test
data:
  APP_NAME: "FakeWorker"
  ENV:       "kwok-test"
  LOG_LEVEL: "debug"
  MAX_WORKERS: "1000"
  API_VERSION: "v1"
